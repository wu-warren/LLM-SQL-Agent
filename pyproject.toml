[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I"]  # errors, flakes, import sorting
ignore = ["E501"]         # don't fight long lines today

[tool.ruff.format]
quote-style = "double"
